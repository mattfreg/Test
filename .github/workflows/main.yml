name: Run CHOpt_linux

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-chopt:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Make CHOpt_linux executable
      run: chmod +x ./CHOpt_linux

    - name: Run Python script to execute CHOpt_linux
      run: |
        python3 run_chopt_linux.py

    - name: List files for debugging
      run: ls -la

    - name: Commit and push results
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add test/output.txt output.png
        git commit -m "Add CHOpt_linux output and png"
        git push